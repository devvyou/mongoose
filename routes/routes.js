const { Router } = require('express');
const router = Router();

// Model retrieved
const User = require('../models/User_model')

router.get('/', (req, res) => {

    /* 

        User.find(),

        User.findOne(),

        User.findById(),

        User.findByIdAndUpdate(),

        User.findByAndDelete(),

        User.update(),

        User.updateMany(),

        User.updateOne()

        User.delete(),

        User.deleteOne(),

        User.deleteMany()

    */

})

module.exports = router;